# @package _global_
defaults:
  - override /dataset: file
  - override /diffusion: edm
  - _self_

name: point_cloud

dataset:
  name: point_cloud

training:
  batch_size: 8

validation:
  batch_size: 16

spectral:
  num_features: 2
