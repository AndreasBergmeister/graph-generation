# @package _global_
defaults:
  - override /dataset: file
  - override /diffusion: edm
  - override /method: one_shot
  - _self_

name: tree_interpolation

dataset:
  name: tree_interpolation

training:
  batch_size: 8

validation:
  batch_size: 40
  interval: 50_000
  per_graph_size: True
