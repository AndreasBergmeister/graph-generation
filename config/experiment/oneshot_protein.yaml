# @package _global_
defaults:
  - override /dataset: file
  - override /diffusion: edm
  - override /method: one_shot
  - _self_

name: oneshot_protein

dataset:
  name: protein

training:
  batch_size: 2

validation:
  batch_size: 4
  interval: 50_000
