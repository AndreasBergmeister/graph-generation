# @package _global_
defaults:
  - override /dataset: file
  - override /diffusion: edm
  - _self_

name: sbm

dataset:
  name: sbm

training:
  batch_size: 16

validation:
  batch_size: 40

spectral:
  num_features: 0