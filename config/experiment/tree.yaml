# @package _global_
defaults:
  - override /dataset: file
  - override /diffusion: edm
  - _self_

name: tree

dataset:
  name: tree

training:
  batch_size: 32

validation:
  batch_size: 40

spectral:
  num_features: 2
